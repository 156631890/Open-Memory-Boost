interface:
  display_name: "Open Memory Boost"
  short_description: "Local memory engine for facts, preferences, decisions, and developer integrations"
  default_prompt: "Use $open-memory-boost to record, retrieve, compact, export, and update long-term user facts, preferences, and decisions in a local Markdown memory store."

policy:
  allow_implicit_invocation: true
